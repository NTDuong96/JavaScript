var colors=['white','black','custom'];
// Tạo mảng colors với 3 giá trị tương ứng

var el=document.getElementById('colors');
el.textContent=colors[0];
// Lấy các el có id='color' và trỏ tới giá trị đầu tiên ([0] = 'white')